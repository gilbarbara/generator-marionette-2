{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.1.0",
  "dependencies": {<% if (compassBootstrap) { %>
    "bootstrap-sass-official": "~3.2",<% } %>
    "jquery": "~2.1",
    "lodash": "~2.4",
    "backbone": "~1.1",
    "marionette": "~2.0",
    "modernizr": "~2.8"<% if (templateFramework === 'handlebars') { %>,
    "handlebars": "~1.3"<% } %>
  },
  "devDependencies": {}
}
